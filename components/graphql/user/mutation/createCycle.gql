mutation CreateCycle(
  $valueUSD: Int!
  $beginDate: DateTime!
  $finishDate: DateTime!
) {
  createCycle(
    data: {
      valueUSD: $valueUSD
      beginDate: $beginDate
      finishDate: $finishDate
    }
  ) {
    field
    message
  }
}
